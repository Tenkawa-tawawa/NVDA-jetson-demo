# 實作範例 (2)：YOLOv8 加速實戰 (FP32 vs FP16)

## 🎯 實作意義 (為什麼要做這個？)
1.  **見證 GPU 的威力**：這是 Jetson 開發板最有價值的地方。我們要比較 **FP32 (單精度)** 與 **FP16 (半精度)** 的速度差異。
2.  **認識 Tensor Cores**：NVIDIA GPU (Ampere 架構) 裡有專門處理 FP16 的硬體單元。開啟 FP16 後，理論上速度會大幅提升，且幾乎不影響準確度。
3.  **實務應用**：YOLO 是主流的物件偵測模型，學會這招代表你能做出更即時 (Real-time) 的應用。

## 🛠️ 事前準備
安裝 YOLO 套件：
\`\`\`bash
pip install ultralytics
\`\`\`

## 🚀 實作步驟
1.  **匯出 ONNX**：
    \`\`\`bash
    yolo export model=yolov8n.pt format=onnx opset=13
    \`\`\`
2.  **跑 FP32 (慢)**：
    \`\`\`bash
    trtexec --onnx=yolov8n.onnx --saveEngine=yolov8n_fp32.plan --dumpProfile > yolo_fp32.log
    \`\`\`
3.  **跑 FP16 (快)**：
    \`\`\`bash
    trtexec --onnx=yolov8n.onnx --saveEngine=yolov8n_fp16.plan --fp16 --dumpProfile > yolo_fp16.log
    \`\`\`

## 📊 結果觀察
比較兩個 log 檔最後的 \`Mean Latency\`。
* **預期**：FP16 應該比 FP32 快約 **2 倍** (例如 8ms -> 4ms)。
